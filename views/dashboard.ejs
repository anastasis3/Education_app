<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Dashboard</title>
  <style>
    /* Clean, professional style */
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background-color: #f5f7fa; /* very light gray-blue */
      color: #2c3e50; /* dark slate blue */
      margin: 0;
      padding: 2rem;
      display: flex;
      justify-content: center;
      min-height: 100vh;
      align-items: center;
    }

    main {
      max-width: 480px;
      width: 100%;
      background: #ffffff; /* white */
      padding: 2rem 2.5rem;
      border-radius: 8px;
      box-shadow: 0 1px 4px rgba(44, 62, 80, 0.1); /* subtle shadow */
      text-align: center;
    }

    h1 {
      font-weight: 600;
      font-size: 1.7rem;
      margin-bottom: 1.5rem;
      color: #34495e; /* slightly lighter dark blue */
    }

    h2 {
      color: #4a6fa5; /* muted blue */
      font-weight: 600;
      margin-top: 2rem;
      margin-bottom: 1.5rem;
      font-size: 1.3rem;
    }

    ul {
      list-style: none;
      padding: 0;
      margin: 0 auto;
      max-width: 280px; /* limit button container width */
    }

    li {
      margin-bottom: 1rem;
    }

    a {
      display: block;
      width: 100%;
      padding: 0.7rem 1rem;
      background: linear-gradient(90deg, #7b68ee, #4169e1); /* soft purple-blue gradient */
      color: #ecf0f1; /* light text */
      font-weight: 600;
      font-size: 1rem;
      border-radius: 6px;
      text-decoration: none;
      text-align: center;
      transition: background-color 0.3s ease;
      user-select: none;
      box-shadow: none;
      border: none;
    }

    a:hover,
    a:focus {
      background: #5a4fcf; /* darker purple on hover */
      outline: none;
      cursor: pointer;
    }

    p {
      color: #7f8c8d; /* gray text */
      font-weight: 600;
      margin-top: 2rem;
      font-size: 1rem;
    }
  </style>
</head>
<body>
  <main>
    <h1>Welcome, <%= user.email %>!</h1>

    <% if (user.role === 'teacher') { %>
      <h2>Teacher Dashboard</h2>
      <ul>
        <li><a href="/forms">List of Created Forms</a></li>
        <li><a href="/create-form">Create Assignment</a></li>
        <li><a href="/results">View Results</a></li>
      </ul>
    <% } else if (user.role === 'student') { %>
      <h2>Student Form</h2>
      <ul>
        <li><a href="/active-form">Fill Active Assignment</a></li>
        <li><a href="/grades">View Grades</a></li>
      </ul>
    <% } else { %>
      <p>Unsupported role</p>
    <% } %>
  </main>
</body>
</html>
